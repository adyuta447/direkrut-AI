fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-multipart==0.0.31

# Provider AI (model-agnostic, lihat app/providers.py)
openai==1.51.0
google-generativeai==0.8.2
groq==0.11.0

# Database & vector search
asyncpg==0.30.0
pgvector==0.3.4

# Cache & rate limiting (Redis)
redis==5.1.1

# Config typed dari env (pydantic-settings, bukan os.getenv() manual)
pydantic-settings==2.6.1

# Object storage (MinIO lokal / S3-compatible di prod)
boto3==1.35.61

# Ekstraksi teks PDF buat CV parsing -- pure Python, gak butuh system dep
pypdf==6.15.0

# Serving classifier kategori CV lokal (model ONNX hasil fine-tune di Colab,
# lihat ml/train_cv_classifier.py + app/routers/cv_classifier.py). SENGAJA
# onnxruntime (CPU, puluhan MB) BUKAN torch (~2GB) biar image tetap ramping &
# muat di dyno yang sekarang -- ini yang bikin fitur ini murah di Heroku.
onnxruntime==1.19.2
tokenizers==0.20.1
numpy==1.26.4

# Utilitas
python-dotenv==1.2.2
httpx==0.27.2
